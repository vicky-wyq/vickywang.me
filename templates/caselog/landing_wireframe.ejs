<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Vicky's UX Portfolio</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg: #fff;
      --text: #1a1a1a;
      --highlight: #ffda6a;
      --tag-bg: #f2f2f2;
      --tag-text: #333;
      --focus-text: #666;
      --card-bg: #f9f9f9;
      --border-radius: 12px;
    }

    * { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      font-family: 'Inter', sans-serif;
      background: var(--bg);
      color: var(--text);
      line-height: 1.6;
      padding: 2rem;
      max-width: 900px;
      margin: auto;
    }

    h1 {
      font-size: 2rem;
      font-weight: 700;
      margin-bottom: 1rem;
    }

    .section-header {
      font-size: 1.5rem;
      margin: 2rem 0 1rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .project {
      background: var(--card-bg);
      padding: 1.25rem 1.5rem;
      border-radius: var(--border-radius);
      margin-bottom: 1.5rem;
    }

    .project h3 {
      font-size: 1.2rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
    }

    .tag {
      display: inline-block;
      background: var(--tag-bg);
      color: var(--tag-text);
      font-size: 0.75rem;
      padding: 0.25rem 0.5rem;
      border-radius: 6px;
      margin-bottom: 0.5rem;
    }

    ul {
      margin-left: 1rem;
      margin-bottom: 0.75rem;
    }

    .focus {
      font-size: 0.9rem;
      color: var(--focus-text);
      margin-bottom: 1rem;
    }

    .button {
      display: inline-block;
      text-decoration: none;
      background: #000;
      color: #fff;
      padding: 0.5rem 1rem;
      border-radius: 8px;
      font-size: 0.85rem;
      margin-top: 0.5rem;
    }
  </style>
</head>
<body>
  <h1>
    👋 Hi, I’m Vicky — a UX designer focused on clean, scalable experiences that drive real impact.
  </h1>
  <p style="margin-top: 0.5rem;">
    I’ve redesigned full rental flows for mobile marketplaces, improved onboarding and checkout in both B2C and B2B contexts, and built design systems that scale. Currently deepening my frontend skills in JavaScript and React to better bridge design and development.
  </p>

  <div class="section-header">🍼 BabyQuip (2023–2025)</div>
  <p>Redesigning the BabyQuip mobile rental experience — from fragmented provider-first navigation to a simplified, gear-first flow that supports users from landing to checkout.</p>

  <div class="project">
    <div class="tag">consumer facing mobile app</div>
    <h3>Customer Rental Experience</h3>
    <ul>
      <li>Simplified landing → reduced cognitive load</li>
      <li>Increased checkout conversion by X%</li>
      <li>Designed QP selection to match user goals faster</li>
    </ul>
    <div class="focus">→ Focus: Improve mobile conversion by simplifying key decisions in the rental flow.</div>
    <a class="button" href="#">View Project</a>
  </div>

  <div class="project">
    <div class="tag">partner / b2b website</div>
    <h3>Inventory Management</h3>
    <ul>
      <li>Reorganized QP dashboard</li>
      <li>Improved tag and category visibility</li>
      <li>Designed scalable structure for future tools</li>
    </ul>
    <div class="focus">→ Focus: Streamline QP operations by reducing manual overhead and improving dashboard clarity.</div>
    <a class="button" href="#">View Project</a>
  </div>

  <!-- Add BookYourPet and LASO sections similarly -->

</body>
</html>
