<!-- Main -->

<%- include(`${templatePath}/shared/block_nav_header`, {
    type:'main', 
    currentPage: '',
    PageUrl:`${websitePath}` 
    }) %>


<!-- Back + Text -->
<%- include(`${templatePath}/shared/block_nav_header`, {
    type:'back', 
    currentPage: '',
    PageUrl:`${websitePath}` 
    }) %>

<!-- Back + Text -->
<%- include(`${templatePath}/shared/block_nav_header`, {
    type:'backText', 
    currentPage: '',
    PageUrl:`${websitePath}` 
    }) %>


<!-- Logo + Text -->
<%- include(`${templatePath}/shared/block_nav_header`, {
    type:'logoText', 
    currentPage: '',
    PageUrl:`${websitePath}` 
    }) %>